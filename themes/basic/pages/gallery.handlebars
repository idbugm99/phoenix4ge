<!-- Universal Gallery System for Basic Theme -->
{{{renderUniversalGallery modelSlug previewTheme=previewThemeId}}}

<!-- Load Universal Gallery System CSS and JavaScript -->
<link rel="stylesheet" href="/templates/universal/gallery-styles.css">
<script src="/templates/universal/gallery-script.js"></script>

<!-- Basic Theme Gallery CSS Variables Override -->
<style>
:root {
    /* Use theme colors from database instead of hard-coded values */
    --gallery-primary: var(--text-accent);
    --gallery-primary-hover: var(--text-accent-hover);
    --gallery-secondary: var(--text-subtle);
    --gallery-accent: var(--text-accent);
    --gallery-background: var(--theme-bg-primary);
    --gallery-surface: var(--theme-bg-light);
    --gallery-text: var(--text-base);
    --gallery-text-muted: var(--text-subtle);
    --gallery-border: var(--theme-border-light);
    --gallery-shadow: rgba(0, 0, 0, 0.1);
    --gallery-shadow-hover: rgba(0, 0, 0, 0.15);

    --gallery-border-radius: 8px;
    --gallery-border-radius-large: 12px;
    --gallery-grid-columns-desktop: 3;
    --gallery-transition-duration: 0.3s;
    --gallery-hover-lift: -2px;
}
</style>